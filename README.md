`.env` file example(used by default with default `docker-compose.yaml`)

``` dotenv
# For nonDocker
# DATABASE_URL=postgres://admin:qwe123@127.0.0.1:5432/database

LISTEN=0.0.0.0
PORT=3229
POSTGRES_USER=admin
POSTGRES_PASSWORD=qwe123
POSTGRES_DB=database
RUST_LOG=info
```
